services:
  - type: web
    name: meishang-tool-station
    env: node
    buildCommand: npm --prefix web ci --include=dev && npm --prefix web run build && npm --prefix server ci --omit=dev
    startCommand: npm --prefix server start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
